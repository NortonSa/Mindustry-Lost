const Chimera = extendContent(HoverUnit,"aerial-chimera"{

	//chama DrawEngine
	drawUnder(){
		DrawEngine(){
			Draw.color(Color.valueOf("#65fff2"));
			Draw.color(Color.white);
			Draw.color();
		}
	};

})
